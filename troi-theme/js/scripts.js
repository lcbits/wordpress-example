/*$(document).ready(function() {
	alert('test');
})*/
(function($) {
	// wp.customize( 'primary_color', function( value ) {
	// 	value.bind( function( newval ) {
	// 		if (newval != '') {
	// 			document.documentElement.style.setProperty('--color_primary', newval);
	// 		}
	// 	} );
	// } );

	// wp.customize( 'web_background', function( value ) {
	// 	value.bind( function( newval ) {
	// 		if (newval != '') {
	// 			document.documentElement.style.setProperty('--color_web_bg', newval);
	// 		}
	// 	} );
	// } );

	// wp.customize( 'cta_block_background', function( value ) {
	// 	value.bind( function( newval ) {
	// 		if (newval != '') {
	// 			document.documentElement.style.setProperty('--color_black_approx', newval);
	// 		}
	// 	} );
	// } );
	// wp.customize( 'footer_background', function( value ) {
	// 	value.bind( function( newval ) {
	// 		if (newval != '') {
	// 			document.documentElement.style.setProperty('--color_footer_bg', newval);
	// 		}
	// 	} );
	// } );

	// wp.customize( 'button_hover', function( value ) {
	// 	value.bind( function( newval ) {
	// 		if (newval != '') {
	// 			document.documentElement.style.setProperty('--color_button_hover', newval);
	// 		}
	// 	} );
	// } );

	// wp.customize( 'button_active', function( value ) {
	// 	value.bind( function( newval ) {
	// 		if (newval != '') {
	// 			document.documentElement.style.setProperty('--color_button_active', newval);
	// 		}
	// 	} );
	// } );


})(jQuery)